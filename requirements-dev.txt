# Development and testing dependencies — NOT required at runtime.
# Install with: pip install -r requirements-dev.txt
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0        # already in requirements.txt; listed here for test client
